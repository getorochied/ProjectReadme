{% extends 'base.html.twig' %}

{% block title %}Utilisateurs - README MiNET{% endblock %}

{% block body %}
<!-- Users Section-->
<section class="page-section portfolio" id="users">
    <div class="container">
        <!-- Section Heading-->
        <h2 class="page-section-heading text-center text-uppercase text-secondary mb-0">
            <i class="fas fa-users"></i> Utilisateurs
        </h2>
        <div class="divider-custom">
            <div class="divider-custom-line"></div>
            <div class="divider-custom-icon"><i class="fas fa-star"></i></div>
            <div class="divider-custom-line"></div>
        </div>
        
        <!-- Users List -->
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="card shadow">
                    <div class="card-body">
                        <table class="table table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th style="width: 10%;">ID</th>
                                    <th style="width: 30%;">Nom d'utilisateur</th>
                                    <th style="width: 20%;">Email</th>
                                    <th style="width: 20%;" class="text-center">Portfolio</th>
                                    <th style="width: 20%;" class="text-center">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for user in users %}
                                <tr>
                                    <td><strong>#{{ user.id }}</strong></td>
                                    <td>
                                        <i class="fas fa-user text-primary me-2"></i>
                                        <strong>{{ user.username }}</strong>
                                    </td>
                                    <td>
                                        <i class="fas fa-envelope text-muted me-1"></i>
                                        <small class="text-muted">{{ user.email }}</small>
                                    </td>
                                    <td class="text-center">
                                        {% if user.portfolio %}
                                            <span class="badge bg-success">
                                                <i class="fas fa-folder"></i> 
                                                {{ user.portfolio.projects|length }} projet(s)
                                            </span>
                                        {% else %}
                                            <span class="badge bg-secondary">Aucun</span>
                                        {% endif %}
                                    </td>
                                    <td class="text-center">
                                        <a href="{{ path('app_user_show', {'id': user.id}) }}" 
                                           class="btn btn-sm btn-primary">
                                            <i class="fas fa-eye"></i> Voir profil
                                        </a>
                                    </td>
                                </tr>
                                {% else %}
                                <tr>
                                    <td colspan="5" class="text-center text-muted py-4">
                                        <i class="fas fa-info-circle"></i> Aucun utilisateur trouvé
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Back Button -->
        <div class="text-center mt-5">
            <a href="{{ path('app_portfolio_index') }}" class="btn btn-secondary btn-xl">
                <i class="fas fa-arrow-left"></i> Retour aux portfolios
            </a>
        </div>
    </div>
</section>
{% endblock %}
